using System.Collections;
using UnityEngine;


public static class Calc
{
    /*    public static T Approach<T>(T now,T target,T add) where T:struct
        {
            dynamic temp = now;
            dynamic end = target;
            dynamic adds = add;
            temp += adds;
            if ((adds > 0 && temp > end) || (adds < 0 && temp < end))
                temp = end;
            return (T)temp;
        }*/

    
}